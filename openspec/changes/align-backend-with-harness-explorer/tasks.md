## 1. 满足前端评审后的实施前置条件

- [ ] 1.1 确认 `add-harness-architecture-explorer` 已实现并向用户展示效果，记录用户明确决定继续本后端 change 的依据；没有继续决定时停止在规划阶段，不实施后续任务。
- [ ] 1.2 根据前端评审结果核对组件分组、职责、业务背景、任务目标和回退需求；如契约变化，先更新本 change 的 proposal/design/specs/tasks。

## 2. 完整组件目录与一致性校验

- [ ] 2.1 在现有 web 模块定义公开组件目录响应模型，沿用 11 个 ID，明确 description、groupId、coverage 的类型和有界内容。
- [ ] 2.2 将组件职责与分组集中到服务端目录，保留 capability 内 id/title 兼容结构，从主要/支撑关系推导 coverage。
- [ ] 2.3 添加只读 `GET /api/harness-components`，包含 subagents，不加载凭据、创建工作区或启动 LLM。
- [ ] 2.4 添加目录/API 测试，覆盖唯一且完整 ID、引用存在、名称一致、关系不重叠、覆盖推导、planned 与可用性分离，以及 GET 无运行副作用。

## 3. 补齐逐项准确的公开任务元数据

- [ ] 3.1 扩展 TaskBrief 的 businessBackground/sharedConstraints 和 DemoTask 的 objective，提供兼容默认值并保留现有字段及别名。
- [ ] 3.2 根据公开合成任务填充业务背景和共同约束，逐项声明六 Demo 目标及完成条件，不在教学字段暴露内部 prompt、测试源码或绝对路径。
- [ ] 3.3 校正 Understand 只读分析、Act 仅建议、Prove 有限反馈、Control 受控停止、Continue 会话交接、Ground 本地依据的完成语义，保持实际执行和评价逻辑不变。
- [ ] 3.4 添加任务语义和旧模型构造兼容测试，覆盖六项 objective、Demo 编号与分派匹配、字段默认及现有响应字段保留。

## 4. 接入已验收前端的展示适配层

- [ ] 4.1 在第一阶段现有 API、类型和展示适配层加入可选组件目录及新增任务字段，不修改已验收布局或新增交互。
- [ ] 4.2 校验新目录完整性、ID、组和枚举，优先采用有效服务端元数据；核心目录加载与可选目录请求独立处理。
- [ ] 4.3 保留旧服务/缺字段回退，目录 404、网络失败或非法内容时整体回退并显示简短非阻塞提示，能力运行权限仍取核心目录。
- [ ] 4.4 添加前端接线测试，覆盖新目录成功、旧响应、缺字段、非法目录、可选请求失败不阻塞，以及预览/详情数据一致。

## 5. 验证兼容性并交付

- [ ] 5.1 运行 `uv run python -m pytest -q` 和 `uv run python -m ruff check src tests fixtures`，确认六 Demo、单能力请求限制、结果、审批和 SSE 回归通过，测试不调用真实网络。
- [ ] 5.2 运行前端 `npm run lint`、`npm run test`、`npm run build`，检查新旧后端数据源的兼容路径。
- [ ] 5.3 人工核对已验收界面的 11 组件、六任务、subagents 状态、业务背景和来源回退，确认没有把教学覆盖或任务目标显示为运行成功。
- [ ] 5.4 更新受影响的后端 API 与前端 README，交付契约变化、兼容检查和任何未完成的真实排练项；不扩展至精确组件追踪或新 Demo。
