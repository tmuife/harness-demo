## 1. 建立独立的前端展示数据和选择规则

- [x] 1.1 核对当前六 Demo API、reducer、任务详情和对照测试，确认改动仅限前端及必要说明；保留已有两个未归档 change 的已实现行为。
- [x] 1.2 在 experiment feature 内定义集中展示模型、11 组件教学目录、固定分组/关系布局和缺失字段回退，不新增后端接口请求。
- [x] 1.3 从 capability 的主要/支撑组件反向推导关联 Demo，实现稳定推荐顺序、保留有效选择、无关联清空及 planned 禁用。
- [x] 1.4 规范化业务背景、六项任务目标、两侧条件、完成条件和观察重点，保证 Understand 等任务语义准确且摘要/详情复用同一数据。
- [x] 1.5 添加展示适配与选择规则测试，覆盖上下文的一对多关系、输出解析支撑关系、subagents、planned 和可选字段缺失。

## 2. 实现教学架构首页与组件侧栏

- [x] 2.1 重组首页为公共业务背景、架构主体和组件侧栏，初始无选择时提供引导及禁用开始状态。
- [x] 2.2 用可访问 HTML 节点、CSS 和必要 SVG 连线绘制 11 组件及 Harness 边界，明确外部交互对象与教学架构标识。
- [x] 2.3 实现组件聚焦、主要/支撑关系文字标识和相关连线高亮，子 Agent 展示未覆盖说明，不加入执行动画。
- [x] 2.4 实现侧栏职责、关联 Demo 单选和任务预览，点击节点/实验选项只改变选择，不启动实验。
- [x] 2.5 实现顶部“查看业务背景”和侧栏“查看完整任务”，复用或保持 dialog 的焦点约束、Escape、关闭及焦点恢复。
- [x] 2.6 添加首页交互测试，覆盖六 Demo 可达、切换任务同步、详情一致、无选择和目录错误可重试状态。

## 3. 接入现有实验工作台并保持状态归属

- [x] 3.1 开始按钮继续提交单元素 capability 配置；开始后进入紧凑架构导航与现有对照工作台。
- [x] 3.2 分离浏览组件/Demo 与 experiment.capabilityId，支持完成后返回总览、标明原 Demo 的最近实验入口和再次查看结果。
- [x] 3.3 运行中锁定配置和重复启动，保留只读任务入口，确保 Control 内联审批始终可见且可操作。
- [x] 3.4 将任务观察重点关联到已有阶段或证据区域，缺失证据使用中性状态，保持结论优先和完整记录分层。
- [x] 3.5 保持刷新后的正确实验/审批恢复及 SSE 生命周期，修正本次入口路径中启动失败、恢复过期后无法重新选择或重试的问题。
- [x] 3.6 添加归属和回归测试，覆盖 Act 结果与新选 Control 分离、刷新恢复、运行锁定、审批、终态校准和错误重试。

## 4. 响应式可访问性与交付检查

- [x] 4.1 完成桌面投屏布局、窄屏分组列表、长文案和证据溢出处理，并支持减少动画、可见焦点及非颜色状态标识。
- [x] 4.2 运行 `npm run lint`、`npm run test`、`npm run build`，解决与改动相关的检查失败。
- [x] 4.3 在 1280px、1440px 桌面和约 390px 窄屏人工查看首页、六项任务与对照入口，检查键盘、减少动画及审批可达性，保留用于用户评审的截图。
- [x] 4.4 在可用且已授权的本地环境检查核心接口联动及 Control 审批；真实调用条件不足时如实记录未排练项，不用模拟成功替代验收记录。
- [x] 4.5 按实际入口变化更新 frontend README，交付界面效果、验证结果及后端待收敛字段；不自动实施 `align-backend-with-harness-explorer`，由用户查看效果后决定是否继续。

## 验证记录（2026-09-16）

- 前端 `npm run lint`、`npm run test`（35 tests / 5 files）、`npm run build` 全部通过；`git diff --check` 与 OpenSpec strict 校验通过。
- Playwright 检查 1280px、1440px、390px 下六项 Demo 的选择、任务预览和详情，共 18 组；主页面与节点无横向溢出。检查了键盘焦点、Tab/Escape、关闭恢复、减少动画和移动端任务定位。
- 使用当前本地后端真实运行 Control：`exp_0442d7cebc3c`。PLAIN 与 HARNESS 均通过固定测试；HARNESS 待审批时刷新页面成功恢复，窄屏审批可操作，批准后执行受保护写入，最终 `scopeOk=true`。每侧 3 次模型调用。该结果仅证明本次链路，不是统计性对比。
- 实验完成后无残留可操作审批；观察阶段定位成功；返回总览选择 Act 后，旧结果仍标记为 Control，不混入 Act 任务。
- 真实排练仅运行上述 Control 一组；六 Demo 全量真实 LLM 排练、真实拒绝和真实超时未重复执行。批准/拒绝、冲突、过期与超时历史恢复由确定性前端测试覆盖。
- 页面截图：[桌面首页](screenshots/desktop.png)、[手机首页](screenshots/mobile.png)、[本次 Control 结论](screenshots/control-result.png)。截图均来自本地页面；首页是静态教学元数据，Control 结论来自上述真实运行。
- 后端代码未修改。`align-backend-with-harness-explorer` 未实施，仍等待用户查看前端效果后决定是否继续。
